version: "1.0"

default_action: "block"

servers:
  prod-analytics-sql-01:
    tools:
      # We allow the query tool
      - name: "query_database"
        action: "allow"
        
      - name: "check_health"
        action: "allow"

      - name: "get_schema"
        action: "allow"
